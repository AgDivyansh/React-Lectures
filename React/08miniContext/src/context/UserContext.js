import React, { useCallback } from "react";
const UserContext = React.createContext();
export default UserContext;

// creating  the below providers  
{/* <UserContext>
    <Login/>
    <Card>
        <Data />
    </Card>
</UserContext> */}